export enum EventStatus {
    CONFIRMED = 'CONFIRMED',
    TENTATIVE = 'TENTATIVE',
    CANCELLED = 'CANCELLED',
}

export enum EventTransparency {
    OPAQUE = 'OPAQUE',
    TRANSPARENT = 'TRANSPARENT',
}
